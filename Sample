
<body>
  <app-database-cmp></app-database-cmp>
  <app-ul-tools></app-ul-tools>
</body>

import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'reverse'
})
export class ReversePipe implements PipeTransform {
  transform(value: string): string {
    return value.split('').reverse().join('');
  }
}
<p>{{ 'Angular' | reverse }}</p> <!-- Output: ralugnA -->
import { Component } from '@angular/core';
import { ReversePipe } from '../reverse.pipe';  // adjust the path if needed

@Component({
  selector: 'app-uitools',
  standalone: true,
  imports: [ReversePipe],  // import the pipe here
  templateUrl: './uitools.component.html',
  styleUrls: ['./uitools.component.css']
})
export class UitoolsComponent {}