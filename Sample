import { NgModule, ApplicationRef } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
import { DatabaseCmpComponent } from './database-cmp/database-cmp.component';
import { UlToolsComponent } from './ul-tools/ul-tools.component';

@NgModule({
  declarations: [
    DatabaseCmpComponent,
    UlToolsComponent
  ],
  imports: [ BrowserModule ],
  entryComponents: [DatabaseCmpComponent, UlToolsComponent]
})
export class AppModule {
  ngDoBootstrap(appRef: ApplicationRef) {
    const loadDatabase = true; // Set to false to load UlToolsComponent

    if (loadDatabase) {
      appRef.bootstrap(DatabaseCmpComponent);
    } else {
      appRef.bootstrap(UlToolsComponent);
    }
  }
}